{
  "requestTemplate": {
    "application/json": {
      "principalId": "$context.authorizer.principalId",
      "requestParams": "$input.json('$')",
      "workout_id": "$input.params('workout_id')"
    }
  }
}